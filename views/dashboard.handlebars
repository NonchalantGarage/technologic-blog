<section>
    <h1 class = "text-center">Your Posts</h1>

    <div class="text-center">
        <form class="btn" type="submit"> <a href="/dashboard/create-post">Create Post
    </div>
        
<div class="text-center">

  {{#each posts as |post|}}
  <p>
    <section>

  <div class="card text-center">

    <div class="card-header">
     <h2><a href="/post/{{id}}">{{title}}</a></h2>
      <h4>Posted by
      {{user.username}}
      on
      {{format_date created_at}} | <a href="/post/{{id}}">{{comments.length}} comment</a></div>
      </h4>
      <br />
      {{post_content}}
      <div>
       <form class="btn " type="submit"> <a href="/dashboard/edit/{{post.id}}">Edit post</a></form>
</div>
  </div>

    
</section>
  </p>
  {{/each}}
